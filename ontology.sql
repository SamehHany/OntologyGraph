CREATE DATABASE Ontology

CREATE TABLE Table0
(
	Resource varchar(255),
	Order varchar(255),
	Entity varchar(255),
	Tangible varchar(255),
	Countable varchar(255),
	Move varchar(255)
);

CREATE TABLE Table1
(
	Warehouse varchar(255),
	Store varchar(255),
	Lot varchar(255),
	decimal varchar(255),
	string varchar(255)
);

CREATE TABLE Table2
(
	Counted varchar(255),
	Barcoded varchar(255),
	Document varchar(255),
	Concept varchar(255),
	WorkDocument varchar(255),
	int INT
);

CREATE TABLE Table3
(
	VirtualStore varchar(255),
	Place varchar(255),
	PhysicalStore varchar(255),
	date DATE,
	boolean varchar(255),
	PlainLiteral varchar(255)
);

